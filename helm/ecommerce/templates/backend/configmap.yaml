apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ .Values.backend.config.configMapName }}
  labels:
    app: backend

data:
  {{- range $key, $value := .Values.backend.config.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
